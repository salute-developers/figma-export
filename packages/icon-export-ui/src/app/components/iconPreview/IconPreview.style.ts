import styled from 'styled-components';
import { buttonAccent } from '@salutejs/plasma-tokens-web';

export const StyledIconPreview = styled.div`
    height: 48px;
    width: 100%;
    background: ${buttonAccent};
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
`;

export const StyledImg = styled.img`
    width: 70%;
    height: 70%;
`;
